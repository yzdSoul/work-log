# 工作日志 — 2026-05-19

## 1. 咪咕逆向工程 (Migu Reverse Engineering)

### 目标
逆向分析咪咕视频 Web API 的加密与签名机制。

### 已破解
| 组件 | 算法 | 来源 |
|------|------|------|
| AEP 加密 | AES-128-ECB + PKCS7 | 模块 65538 (CryptoJS) |
| ensign 签名 | MD5 | 模块 62094 (JS-MD5) |
| 请求头注入 | Axios 拦截器 #11 | pugc.js 模块 24747 |

### 关键代码
```javascript
// AES 加密
encrypt(body, key) {
  var keyBytes = CryptoJS.enc.Utf8.parse(key);
  body = JSON.stringify(body);
  var dataBytes = CryptoJS.enc.Utf8.parse(body);
  return CryptoJS.AES.encrypt(dataBytes, keyBytes, {
    mode: CryptoJS.mode.ECB,
    padding: CryptoJS.pad.Pkcs7
  }).toString();
}

// ensign 签名
ensign = MD5(rawBody + secretKey + timestamp).toUpperCase()
```

### 卡住点
- 加密密钥 `m` 从用户登录信息动态获取，非硬编码
- 需要已登录咪咕的 Chrome 实例连接 MCP 截获运行时密钥
- Chrome 自动启动因 sandbox 失败

### 文件备份
- JS 源文件: `~/projects/migu-reverse/js-files/` (42个文件)
- 包含: pugc.js, vue_mgvendor.js, contentManage.chunk.js

### 待办
1. 手动启动已登录咪咕的 Chrome: `google-chrome --remote-debugging-port=9222`
2. MCP 连接抓动态密钥 `m`
3. 写 Node/Python 复现脚本

---

## 2. 每日格言 (daily-motto) 项目更新

### 已提交到 Git
| Commit | 内容 |
|--------|------|
| `c9d322b` | feat: 新增每日冷笑话板块 |
| `46fb257` | feat: 冷笑话投稿功能 |
| `b69bcd5` | perf: 修复 N+1 查询 + 每日缓存 |
| `63ffe5f` | perf: 添加 HTTP 缓存头 |
| `d13250f` | chore: 新增 38 条笑话 (共 84 条) |
| `fcc877f` | feat: Cloudflare Worker + 审核脚本 |
| `5fad760` | docs: README 添加线上地址 |

### 新增文件
- `cloudflare-worker.js` — Cloudflare Worker 反向代理 + 缓存
- `wrangler.toml` — Workers 部署配置
- `scripts/review_quotes.py` — 投稿审核脚本 (MongoDB URI 从环境变量读取)

### 线上地址
- 🌐 [https://yzdsoul-daily-motto.hf.space](https://yzdsoul-daily-motto.hf.space)

---

## 3. 大模型配置更新

### 可用模型 & 余额
| 模型 | 提供商 | 余额 | 状态 |
|------|--------|:----:|:----:|
| deepseek-v4-flash | DeepSeek | ¥10.02 | ✅ |
| kimi-k2.6 | Moonshot | ¥14.35 | ✅ |
| kimi-k2.5 | Moonshot | ¥14.35 | ✅ |
| moonshot-v1-128k | Moonshot | ¥14.35 | ✅ |
| **kimi-for-coding** 🆕 | Kimi Coding | 新 key | ✅ 当前在用 |

### 配置变更
- 默认模型: `deepseek-v4-flash` → `kimi-for-coding`
- 更新 `KIMICODE_API_KEY` 为新的有效 key
- `~/.bashrc` 添加 `MONGODB_URI` 环境变量

---

## 4. 安全修复

### MongoDB 密码处理
- **问题**: `scripts/review_quotes.py` 明文存储 MongoDB 密码
- **修复**: 改为从 `MONGODB_URI` 环境变量读取
- **验证**: MongoDB 连接测试通过 ✅
